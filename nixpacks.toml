[phases.build]
cmds = [
  "cd frontend && NODE_ENV=production npm install && NODE_ENV=production npm run build",
  "mkdir -p static/frontend",
  "cp -r frontend/dist/* static/frontend/"
]

[phases.setup]
nixPkgs = ["nodejs_18", "python311", "redis"]