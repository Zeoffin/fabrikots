{
  "name": "fabrikots",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && mkdir -p static/frontend && cp -r frontend/dist/* static/frontend/"
  },
  "engines": {
    "node": "18.x"
  }
}